# November 3, 2017

Focused on styling with CSS-in-JS using styled-components.

It allows for better component encapsulation.

The performance aspect is something to keep an eye on.

Also need to test it with different browsers.