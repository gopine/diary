# November 30, 2017

Exploring server-sent events (SSE) for real-time updates from the server.

A simple alternative to WebSockets for unidirectional data flow.

Implementing error handling and reconnect logic.

Need to compare it with WebSockets for specific use cases.

Configured a basic SSE server.