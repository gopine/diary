# September 10, 2017

Set up a GraphQL server using Node.js and Express.

Connected the server to a MongoDB database.

Defined a GraphQL schema for the database.

Now I can query the database using GraphQL.

Setting up a GraphQL server takes some effort.