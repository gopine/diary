# September 11, 2017

Built a simple client application using React and GraphQL.

The application fetches data from the GraphQL server and displays it in a user interface.

GraphQL makes it easy to fetch only the data that is needed for the UI.

This can improve the performance of the application.