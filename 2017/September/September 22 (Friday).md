# September 22, 2017

Dockerized my TypeScript project.

Created a Dockerfile that defines the environment for the application.

Built a Docker image from the Dockerfile.

Ran the Docker image in a container.

The application is now running in a consistent environment.

Docker makes application environments consistent.