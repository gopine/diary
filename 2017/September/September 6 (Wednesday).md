# September 6, 2017

Built a simple serverless function using AWS Lambda.

The function receives an HTTP request and returns a JSON response.

Deployed the function to AWS.

Successfully invoked the function using a web browser.