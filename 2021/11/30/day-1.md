"Looking into WebAssembly (Wasm) for improving the performance of computationally intensive tasks.

Compiling C++ code to Wasm and running it in the browser.

Potentially useful for image processing."