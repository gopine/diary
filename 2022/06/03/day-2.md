Exploring CSS Modules.

A way to scope CSS classes locally to components.

It avoids CSS conflicts.

Need to use it to build scalable projects.

Reading the documentation on CSS Modules.

Using CSS Modules in a React project.