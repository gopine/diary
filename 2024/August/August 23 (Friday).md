# August 23, 2024

Experimenting with different UI frameworks.

Svelte looks interesting with its compile-time optimizations.

The component syntax seems a bit different.

Let's see if it provides more performance compared to React.

Svelte's reactivity is surprisingly intuitive.

Built a small demo app with Svelte.