# June 12, 2024

Started exploring the possibility of using GraphQL instead of REST APIs.

Seems to be a good solution for complex data dependencies.

I think I would improve the performance by reducing over-fetching.

Need to explore it further.

Trying out different GraphQL clients for React.

Reading more about GraphQL schema design best practices.

Implemented a basic GraphQL query for fetching user data.