# January 8, 2025

Implemented a new API endpoint using GraphQL.

It's much more efficient than our old REST API.

The client now only fetches the data it needs.

It needs time to get used to GraphQL.